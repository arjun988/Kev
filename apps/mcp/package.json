{
  "name": "@kev-ai/mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server for Kev System One decisions",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "kev-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@kev-ai/sdk": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
