# @kev-ai/server

Decision API for **NotJev : Kev** (playground included).

```bash
npm install -g @kev-ai/server
# or: npx @kev-ai/server
kev-server
```

```bash
KEV_BACKEND=ollama KEV_OLLAMA_MODEL=qwen3.5:9b kev-server
```

Open `http://127.0.0.1:3000/playground/`.
